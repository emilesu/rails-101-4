<div class="col-md-12">
  <div class="group">
    <%= link_to("发表文章", new_group_post_path(@group), class: "btn btn-default pull-right") %>
    <% if current_user && current_user == @group.user %>
    <%= link_to("编辑", edit_group_path(@group), class: "btn btn-success pull-right") %>
    <% end %>
  </div>
  <h2><%= @group.title %></h2>
  <p><%= @group.description %></p>

  <table class="table">
    <thead>
      <tr>
        <td>文章内容</td>
        <td>作者</td>
        <td>发表时间</td>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td><%= post.content %></td>
          <td><%= post.user.email %></td>
          <td><%= post.created_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>
